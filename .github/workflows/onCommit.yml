name: Build

on:
  push:

# Use bash also on Windows (instead of pwsh)
defaults:
  run:
    shell: bash

jobs:
  build:
    strategy:
      fail-fast: false
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3

    - name: Install dependencies
      run: | 
        sudo apt-get update
        sudo apt-get install -y \
          libavfilter-dev \
          libavformat-dev \
          libavcodec-dev \
          libswresample-dev \
          libavutil-dev

    - uses: Jimver/cuda-toolkit@v0.2.8
      id: cuda-toolkit
      with:
        cuda: '11.7.0'
        sub-packages: '["nvcc", "nvtx", "cudart"]'
        method: "network"

    - name: Install CUVID
      run: | 
        # normally you would install them via `apt install cuda`
        sudo apt-get install \
          libnpp-dev-11-7 \
          libnvidia-decode-520 \
          -y

    - name: Build and Install Package
      run: | 
          git submodule update --init --recursive
          python -mpip install . --verbose
          python -mpip install src/PytorchNvCodec --verbose          

    - name: Check Import
      # importing does not work on Windows as nvcuda.dll is required which comes with a Nvidia driver
      run: |
        python -c "import PyNvCodec"

    - name: Build docs
      run: |
        python -mpip install sphinx
        cd docs
        make html

    - name: Upload html docs
      uses: actions/upload-artifact@v3
      with:
        name: sphinx_docs
        path: _build/html/*
        retention-days: 1
